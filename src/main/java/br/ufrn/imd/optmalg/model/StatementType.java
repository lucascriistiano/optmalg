package br.ufrn.imd.optmalg.model;

public enum StatementType {
    METHOD_DECLARATION, VAR_DECLARATION,
    IF, ELSE, ELSE_IF, FOR, WHILE, DO,
    RETURN, BREAK, CONTINUE, SWITCH, CASE, DEFAULT,
    TRY, CATCH, FINALLY,
    OTHER
}